import { Container } from '../components/ui/Container';
import { SectionHeader } from '../components/ui/SectionHeader';

export function BrainyBitesPage() {
  return (
    <Container className="py-20">
      <SectionHeader
        title="Brainy Bites"
        subtitle="Turning curiosity into creativity, one session at a time"
      />
      {/* TODO: Add content */}
    </Container>
  );
}